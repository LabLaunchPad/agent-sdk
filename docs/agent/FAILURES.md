# FAILURES

Known failures and their status. A failure is recorded here the moment it is
observed, not once it is fixed.

Never delete an entry. Resolved failures move to Resolved and keep their
regression test reference — the record is what stops the same failure
recurring.

## Open

| ID  | Observed | Failure       | Severity | Status | Regression test |
| --- | -------- | ------------- | -------- | ------ | --------------- |
| —   | —        | none recorded | —        | —      | —               |

## Resolved

| ID   | Observed   | Failure                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Root cause                                                                                                                                                                                                                                                                                        | Regression test                                                                                                                                                                                                         |
| ---- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F001 | 2026-08-18 | `.context/index.md` and `README.md` stated a validator count (six / five) that disagreed with the actual seven validator files on disk; `pnpm validate` passed 7/7 regardless. Mutation-tested: reverting both files to this exact stale content still produced 7/7 PASS.                                                                                                                                                                                                                                                                                                                                                                   | No mechanism compared a prose claim about the repository to the repository fact it claimed to describe - the seven pre-existing validators each check one artifact's internal correctness, none compare a projection to its source. See `docs/audit/REPOSITORY-INTEGRITY-AUDIT.md` finding A1/A3. | `scripts/repo-tools/test/derived-projection-consistency.unit.test.ts` (Tests A/B)                                                                                                                                       |
| F002 | 2026-08-18 | Three-way live contradiction on the current phase: `.context/index.md` said P1A/"Do not begin Phase 2", `docs/agent/STATE.md` said Phase 1B, machine state (`project.json`) said P2 COMPLETE. `.context/index.md` is the mandated first read (`AGENTS.md` rule C2), so the stalest artifact was the most-read one.                                                                                                                                                                                                                                                                                                                          | Same root cause as F001, applied to phase state specifically; compounded by `9eef033` (2026-08-17) having fixed an earlier instance of the same class one phase before this one recurred - proof that manual repair does not hold.                                                                | `scripts/repo-tools/test/derived-projection-consistency.unit.test.ts` (Test C)                                                                                                                                          |
| F003 | 2026-08-18 | ADR inventory in `.context/index.md` listed only ADR-0001..0008; 8 more (ADR-0009..0016) existed and were unlisted. Research-brief completion stated "9 of 17, 3 UNKNOWN" in one place while `.context/research/gaps.json`'s own structured `remaining_primary`/`remaining_protocols` arrays (both empty) said otherwise - the file contradicted itself.                                                                                                                                                                                                                                                                                    | Same root cause as F001/F002, plus: `.context/research/gaps.json`'s top-level `gap` prose field was never updated when its own `volcengine_baidu_closure_note` was appended, so even the "authoritative" cache entry had two disagreeing claims inside one file.                                  | `scripts/repo-tools/test/derived-projection-consistency.unit.test.ts` (Tests D/E)                                                                                                                                       |
| F004 | 2026-08-18 | `schema-contract-validator`'s custom draft-07 evaluator silently ignored 20 of 21 tested keywords (`oneOf`, `anyOf`, `allOf`, `not`, `$ref`, `format`, `exclusiveMinimum`/`Maximum`, `multipleOf`, `minItems`/`maxItems`, `patternProperties`, and more), every divergence in the fail-open direction - exactly the "projection more permissive than its schema" defect the validator exists to catch, present inside the validator. 8 of 12 everyday Zod constructs (`union`, `nullable`, `discriminatedUnion`, `array().min()`, `positive()`, `record`, `intersection`) emit ignored keywords, so exposure was imminent, not theoretical. | The evaluator's supported vocabulary was implicit (whatever `evaluate()` happened to check) rather than declared, with no assertion that a schema stayed within it - an unrecognised keyword fell through every `if` and returned `true`.                                                         | `scripts/repo-tools/test/schema-safety.unit.test.ts` (12-keyword regression corpus + nested-keyword case + Ajv 8.20.0 differential test)                                                                                |
| F005 | 2026-08-18 | `schema-contract.ts`'s `acceptsUnderProjection` docstring claimed "Ajv is loaded lazily and per-call from the validated package's own dependency tree" - false; the function called the hand-rolled `evaluate()`, Ajv was never imported at runtime.                                                                                                                                                                                                                                                                                                                                                                                        | Same root cause as F001-F003, inside a code comment instead of a document - drift is not format-specific.                                                                                                                                                                                         | None (docstring corrected in place; `derived-projection-consistency` deliberately scopes to `.md` projections, not code comments - see docs/audit/REPOSITORY-INTEGRITY-AUDIT.md "What is deliberately NOT implemented") |

## Recording rules

1. Record on observation, with the actual error output — not a paraphrase.
2. Every resolved failure carries a regression test. No test, not resolved.
3. "Flaky" is not a root cause. A job that died before any test body ran
   (checkout, dependency install, lost runner) may be re-run and noted as such;
   everything else gets diagnosed.
4. Disabling, skipping or quarantining a test to reach green is prohibited.
   | F006 | 2026-08-18 | `derived-projection-consistency-validator` (F001-F003's own control) scanned every `.md` file, so `docs/audit/REPOSITORY-INTEGRITY-AUDIT.md`'s prose _explaining_ the marker convention was parsed as a control artifact and failed the gate. The fix applied at the time was to reword the documentation until the tool passed. | Control had no notion of projection identity: any marker anywhere was treated as a repository claim. Bending a document to satisfy a checker is the same class as "remember to update the README" - the human absorbs the defect instead of the mechanism. | `derived-projection-consistency.unit.test.ts` (registry §12 suite: registry-missing, missing-region, undeclared-region, ignores-unregistered-file) |
   | F007 | 2026-08-18 | Running `node scripts/repo-tools/dist/cli.js validate` directly on Node 22 printed an ordinary `PASS`, despite `.context/state/runtime.json` declaring that a green run off-baseline is not evidence. Recorded `MISSED` in the audit's adversarial pass. | The baseline was enforced only by `package.json`'s `engines` field, which `pnpm` checks; a direct `node` invocation is not a `pnpm` invocation. A decision-critical entry point assumed a wrapper had established its environment. | `toolchain-gate.unit.test.ts` (5 cases incl. patch-level mismatch); CLI now exits `3 = VERIFY_BLOCKED` |
   | F008 | 2026-08-18 | The projection registry introduced for F006 became an unprotected surface: deleting an entry from `projections.json` silenced that projection entirely and no validator noticed, because a registry-driven validator can only check what the registry declares. Found by adversarial mutation of the new control, not by review. | A control whose own configuration is unguarded delegates its coverage decision to whoever edits that configuration. Fixing one class created a narrower instance of the same class one level up. | `derived-projection-consistency.unit.test.ts` ("pins registry membership...") - same meta-guard shape as `ci-coverage.unit.test.ts` |
